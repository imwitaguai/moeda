# Configure no painel da Netlify; nunca salve a chave real neste arquivo.
# A URL precisa conter os marcadores {base} e {key}.
CURRENCY_API_URL_TEMPLATE=https://SEU-PROVEDOR/latest?base_currency={base}&apikey={key}
CURRENCY_API_KEY=adicione_a_chave_somente_na_netlify
